import { GototopDirective } from './gototop.directive';

describe('GototopDirective', () => {
  it('should create an instance', () => {
    const directive = new GototopDirective();
    expect(directive).toBeTruthy();
  });
});
